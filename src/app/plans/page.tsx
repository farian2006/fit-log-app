'use client'
import { GymDataContext } from '@/context/GymContext';
import React, { useContext } from 'react';

const ListedPlans = () => {
    const {} =useContext(GymDataContext)
    return (
        <div>
            
        </div>
    );
};

export default ListedPlans;